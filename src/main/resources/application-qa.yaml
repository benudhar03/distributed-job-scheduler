spring:
  kafka:
    bootstrap-servers: qa-kafka.company.com:9092

  data:
    mongodb:
      uri: mongodb://qa-mongo.company.com:27017/scheduling_db
      database: scheduling_db
      auto-index-creation: true

app:
  environment: qa

logging:
  level:
    root: INFO
